spring:
  application:
    name: dashboard

  datasource:
    url: jdbc:postgresql://localhost:5432/dashboard
    username: dashboard_user
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: ThisIsMyVerySecretDashboardKey2026ThatShouldBeLongEnough123456
  expiration: 3600000

server:
  port: 8080